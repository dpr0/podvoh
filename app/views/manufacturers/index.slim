section.text-center
  .container
    .row
      section.about-section.text-center
        .container
          .row
            - (0..13).each_slice(7) do |part|
              div
                - part.each do |i|
                  = link_to '#' do
                    = image_tag "podvoh/image_part_#{(i+1).to_s.rjust(3, "0")}.jpg", style: 'width: 14%;', class: 'bordered'
    .row
      - @manufacturers.each do |manufacturer|
        .col-sm-3
          = manufacturer.name
          p = link_to manufacturer_path(manufacturer.id) do
            = image_tag "manufacturers/#{manufacturer.code}.jpg", style: 'width: 90%; height: 5.75rem;', class: 'img-fluid img-center'

